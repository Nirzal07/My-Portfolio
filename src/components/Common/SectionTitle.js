import React from "react";

export default function SectionTitle({ title }) {
  return (
    <div>
      <h4 className="text-center text-[36px] font-bold text-green mb-3">
        {title}
      </h4>
    </div>
  );
}
